<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBackground"
    tools:context=".MainActivity">

    <FrameLayout
        android:id="@+id/frame_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </FrameLayout>

<!--    底部導覽欄 -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:background="@drawable/bottom_nav_background_main_activity"
        android:elevation="2dp"
        app:menu="@menu/bottom_menu"
        app:itemActiveIndicatorStyle="@style/Indicator"
        app:itemIconSize="35dp"
        app:itemIconTint="@drawable/item_selector_home_fragment"
        app:itemRippleColor="@android:color/transparent"
        app:itemTextColor="@drawable/item_selector_home_fragment"
        app:labelVisibilityMode="selected"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="25dp" />

</RelativeLayout>